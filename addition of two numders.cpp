#include <stdio.h>

int main() {
    int num1, num2;

    // Read two numbers from input
    scanf("%d %d", &num1, &num2);

    // Calculate sum
    int sum = num1 + num2;

    // Print result
    printf("%d", sum);

    return 0;
}
